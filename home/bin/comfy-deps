#!/usr/bin/env bash
set -e

COMFY="$HOME/AI_Workspace/ComfyUI"

cd "$COMFY/custom_nodes"

find . \
    -path "./.disabled" -prune -o \
    -mindepth 1 -maxdepth 1 -type d -print0 |
while IFS= read -r -d '' dir
do
    if [[ -f "$dir/requirements.txt" ]]; then
        echo
        echo "======================================="
        echo "${dir#./}"
        echo "======================================="

        uv pip install -r "$dir/requirements.txt"
    fi
done
